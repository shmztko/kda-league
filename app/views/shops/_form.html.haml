= form_for @shop do |f|
  - if @shop.errors.any?
    .alert.alert-error    
      %ul
        - @shop.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name

  .actions
    = f.submit 'Save'
