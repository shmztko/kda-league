= form_for @game_result do |f|
  - if @game_result.errors.any?
    #error_explanation
      %h2= "#{pluralize(@game_result.errors.count, "error")} prohibited this game_result from being saved:"
      %ul
        - @game_result.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
